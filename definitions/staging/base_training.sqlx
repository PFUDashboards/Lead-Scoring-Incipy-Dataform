config {
  type: "view",
  schema: "BQ_PFU_INCIPY",
  name: "base_training"
}

SELECT *
FROM ${ref("base")}

WHERE event_timestamp >= UNIX_MICROS(
  TIMESTAMP_SUB(CURRENT_TIMESTAMP(), INTERVAL 730 DAY))